# resume-tweaker
Use text from previous job applications to generate customized resume/cover letter for new postings.  Uses AI to select the text to use, but not to write the majority of the text, since all it's doing is remixing human-written text.
