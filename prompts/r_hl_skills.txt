Under "PREVIOUS EXAMPLES" below is a list of "headline" skills lists - which appear immediately underneath the title of my resume - used in previous job applications.  In each case, 3 items were selected to align with the details of the position being applied for, and combined into a single string with pipe separators.  For example, for a role involving business analytics and machine learning that requires some skill in problem framing and methodology development, the output might be: "Problem framing | Decision Intelligence | Machine Learning".
Your task is to generate the "headline" skills list that I should use when applying for the new job posting.  Because I want to reuse text that I've already checked wherever possible, please follow this process:
1. From the PREVIOUS EXAMPLES list, select 3 items that most closely align with the job posting.  Avoid selecting items that are too similar in meaning or relate to similar aspects of the role (e.g. "ACTIONABLE INTELLIGENCE" and "DECISION INTELLIGENCE").
2. If the selected items adequately represent the skills needed for the role, return the string "UNCHANGED\n" + the pipe-delimited string.
3. Otherwise - i.e. if the selected items leave out an important aspect of the role - create a list that includes newly generated text, still using components from PREVIOUS EXAMPLES as much as possible.  Return the string "NEWTEXT\n" + the pipe-delimited string.
