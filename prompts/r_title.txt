Under "PREVIOUS EXAMPLES" below is a list of resume titles used in previous job applications, selected to align with the details of the position being applied for.  For example, for a role requiring a combination of business analytics, machine learning and team leadership, a fitting resume title might be "DATA SCIENCE & ANALYTICS LEADER".
Your task is to generate the title text that I should use when applying for the new job posting.  Because I want to reuse text that I've already checked wherever possible, please follow this process:
1. From the PREVIOUS EXAMPLES list, identify the title that most closely aligns with the details of the job posting (not just the job title).
2. If the selected title fits well with the job posting, return the string "UNCHANGED\n" + the selected text with no changes.
3. Otherwise, if you think a fitting title can be composed by combining parts of the PREVIOUS EXAMPLES, return the string "REMIXED\n" + the text you composed.
4. Otherwise, create a title that includes newly generated text, still using components from PREVIOUS EXAMPLES as much as possible.  Return the string "NEWTEXT\n" + the text you composed.
