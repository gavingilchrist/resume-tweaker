Under "PREVIOUS EXAMPLES" below is a list of achievements from a previous job role that have been included in my resume for previous job applications.  For each achievement, the description may be adjusted to align with the details of the position being applied for.  The order in which the achievements were listed was also customized, with those most relevant to the job being applied for placed closest to the top of the list.  The PREVIOUS EXAMPLES list is organized with groups of descriptions relating to the same achievement grouped together, with the string '-----' used to separate different groups.
Each description should ideally be no more than 230 characters long.  Descriptions should not be substantially longer or shorter, unless it yields significant benefit in terms of improved alignment to the job description.  (Some entries in the PREVIOUS EXAMPLES do exceed this, but you should aim to generate shorter output if possible.)
Your task is to generate the version of this list of achievements I should use when applying for the new job posting.  Please follow this process:
1. Considering the achievements first, determine in which order they should appear in the output so that those most relevant to the job posting are closest to the top of the list.  Assign each achievement a ranking number to indicate where it should be placed in the final list.
2. For each achievement, in the order they appear in the PREVIOUS EXAMPLES list, generate the output using this sub-process:
(a) Select the version of the description from the PREVIOUS EXAMPLES list that most closely aligns with the job posting.
(b) If the version you selected seems appropriate for the job posting, return the string "UNCHANGED\n" + the selected description with no changes + '\n' + the ranking number for that achievement.
(c) Otherwise, create a description that includes newly generated text where needed, still using components from PREVIOUS EXAMPLES as much as possible.  DO NOT add embellishments - including key words from the job description - that are not directly supported or strongly implied by the PREVIOUS EXAMPLES for that specific achievement.  Follow the length guidelines described above.  Return the string "REWRITTEN\n" + the description you created + '\n' + the ranking number for that achievement.
When finished, you should have generated output for each achievement represented by a group of descriptions in the PREVIOUS EXAMPLES list.  Please insert '\n\n' between each of these outputs.
