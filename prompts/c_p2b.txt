Under "PREVIOUS EXAMPLES" below is a list of achievements from throughout my career, listed as "showcase" items in the cover letter used for previous job applications.  The cover letter would include 3 or 4 items, selected and ordered to align with the details of the position being applied for, and different versions of the wording were used for each selected item - also to align to the job being applied for.  The PREVIOUS EXAMPLES list is organized with groups of descriptions relating to the same achievement grouped together, with the string '-----' used to separate different groups.
1. Considering the achievements first, identify 3 or 4 items that collectively align with the job posting, and determine in which order they should appear in the output so that those most relevant to the job posting are closest to the top of the list.  Assign each achievement a ranking number to indicate where it should be placed in the final list.  For achievements not among the 3-4 items selected, assign the ranking number 0.
2. For each achievement, in the order they appear in the PREVIOUS EXAMPLES list, generate the output using this sub-process:
(a) If the ranking number is 0 - i.e. the achievement isn't one of the 3-4 selected to be included - return the string "NOT SELECTED\n" and stop.
(b) Otherwise, select the version of the description from the PREVIOUS EXAMPLES list that most closely aligns with the job posting.
(c) If the version you selected seems appropriate for the job posting, return the string "UNCHANGED\n" + the selected description with no changes + '\n' + the ranking number for that achievement.
(d) Otherwise, create a description that includes newly generated text where needed, still using components from PREVIOUS EXAMPLES as much as possible.  DO NOT add embellishments that are not directly supported by material in PREVIOUS EXAMPLES, and keep the length of the new text consistent with PREVIOUS EXAMPLES.  Return the string "REWRITTEN\n" + the description you created + '\n' + the ranking number for that achievement.
When finished, you should have generated output for each achievement represented by a group of descriptions in the PREVIOUS EXAMPLES list.  Please insert '\n\n' between each of these outputs.
